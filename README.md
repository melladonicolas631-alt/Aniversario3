
  # Digital Memory Book

  This is a code bundle for Digital Memory Book. The original project is available at https://www.figma.com/design/KgK1gI0URhYQ670HijSY6P/Digital-Memory-Book.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  